<form method="post" action="{{ path('print_request_delete', {'id': print_request.id}) }}" onsubmit="return confirm('{{ 'print_request.delete.confirm'|trans }}');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-print-request-' ~ print_request.id) }}">
    <button class="btn btn-danger"><span class="fa fa-trash"></span> {{ 'common.action.delete'|trans }}</button>
</form>
